#include "main.h"
/**
 * _abs - compute the abs value of an int
 * @n: is the int being checked
 * code by Babatunde
 * Return: the abs of the int
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
